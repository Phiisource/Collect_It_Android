<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.utt.if26.collectit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name=".ui.lots.AjouteLotActivity"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name=".ui.lots.ModifieLotActivity"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name=".ui.login.InscriptionActivity"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name=".ui.login.ProfilActivity"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name=".ui.utilisateur.ModifierPointsActivity"
            android:label="@string/app_name">
        </activity>
    </application>

</manifest>